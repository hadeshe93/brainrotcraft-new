# CSV 域名统计工具 - 设计方案

## 项目概述

本工具用于从多份 CSV 文件中提取 "Source url" 列的域名信息，统计域名出现频次，过滤高频域名并输出到 CSV 文件。

## 需求规格

### 输入参数
1. **目录路径** (string): 包含多份 CSV 文件的文件夹绝对路径
2. **最小出现次数** (number): 域名过滤阈值，例如 `2` 表示域名至少出现 2 次

### 输出
- **返回值**: 布尔值，表示写入是否成功
- **文件输出**: `tools/output.csv`

### 功能需求
1. 扫描指定目录下的所有 CSV 文件
2. 从每个文件的 "Source url" 列提取 URL
3. 从 URL 中提取域名（保留完整域名，不做规范化）
4. 统计每个域名的出现次数
5. 过滤出现次数 >= n 的域名
6. 按出现次数从高到低排序
7. 为每个域名随机选择最多 3 个示例 URL
8. 输出到 CSV 文件
9. 打印处理进度
10. 输出总结信息

### 输出文件格式
```csv
域名地址,出现次数,示例url1,示例url2,示例url3
example.com,15,https://example.com/page1,https://example.com/page2,https://example.com/page3
test.com,8,https://test.com/a,https://test.com/b,
another.com,3,https://another.com/x,,
```

### 非功能需求
- CSV 列名: `Source url` (精确匹配，S 和 u 大写，中间有空格)
- 文件编码: 自动检测
- 域名处理: 保留完整域名 (www.example.com 和 example.com 视为不同)
- 示例 URL 列: 固定 3 列，不足时留空

## 架构设计 (SOLID 原则)

### 设计原则
- **单一职责原则 (SRP)**: 每个函数/类只负责一个功能
- **开闭原则 (OCP)**: 易于扩展新功能，无需修改现有代码
- **里氏替换原则 (LSP)**: 使用接口和抽象，便于替换实现
- **接口隔离原则 (ISP)**: 接口精简，依赖最小化
- **依赖倒置原则 (DIP)**: 依赖抽象接口而非具体实现

### 分层架构

```
┌─────────────────────────────────────┐
│   主协调层 (Orchestration Layer)    │
│   processCsvDomains()               │
└────────────┬────────────────────────┘
             │
    ┌────────┴────────┐
    ↓                 ↓
┌─────────────┐  ┌──────────────┐
│ 文件操作层   │  │ 进度跟踪层    │
│ File Layer  │  │ Progress     │
└──────┬──────┘  └──────────────┘
       │
       ↓
┌─────────────────────────────────┐
│      数据提取层                  │
│   Data Extraction Layer         │
└────────────┬────────────────────┘
             │
             ↓
┌─────────────────────────────────┐
│      统计分析层                  │
│   Statistics Layer              │
└────────────┬────────────────────┘
             │
             ↓
┌─────────────────────────────────┐
│      输出格式化层                │
│   Output Layer                  │
└─────────────────────────────────┘
```

## 模块设计

### 1. 类型定义模块 (Types)

```typescript
// CSV 行数据类型
interface CsvRow {
  [key: string]: string;
}

// 域名统计信息
interface DomainStats {
  domain: string;
  count: number;
  urls: string[];
}

// 域名条目（Map 存储）
interface DomainEntry {
  count: number;
  urls: Set<string>;
}

// 输出行格式
interface OutputRow {
  domain: string;
  count: number;
  sampleUrl1: string;
  sampleUrl2: string;
  sampleUrl3: string;
}

// 处理总结信息
interface ProcessSummary {
  totalFiles: number;
  totalRows: number;
  totalDomains: number;
  filteredDomains: number;
}
```

### 2. 文件操作层 (File Operations)

#### 2.1 `getAllCsvFiles(dirPath: string): Promise<string[]>`
**职责**: 扫描目录获取所有 .csv 文件路径
- 输入: 目录路径
- 输出: CSV 文件路径数组
- 依赖: Node.js `fs/promises`, `path`

#### 2.2 `detectFileEncoding(filePath: string): Promise<string>`
**职责**: 自动检测文件编码
- 输入: 文件路径
- 输出: 编码名称 (如 'utf-8', 'gbk')
- 依赖: `chardet` 库
- 错误处理: 检测失败时默认返回 'utf-8'

#### 2.3 `readAndParseCsv(filePath: string, encoding: string): Promise<CsvRow[]>`
**职责**: 读取并解析单个 CSV 文件
- 输入: 文件路径, 编码格式
- 输出: CSV 行数据数组
- 依赖: `csv-parse`, `iconv-lite`
- 错误处理: 解析失败抛出错误

### 3. 数据提取层 (Data Extraction)

#### 3.1 `extractDomainFromUrl(url: string): string | null`
**职责**: 从 URL 提取域名（纯函数）
- 输入: URL 字符串
- 输出: 域名字符串 或 null（无效 URL）
- 实现: 使用 Node.js URL API
- 特点: 无副作用，可独立测试

#### 3.2 `extractSourceUrls(rows: CsvRow[], columnName: string): string[]`
**职责**: 从 CSV 行中提取指定列的 URL
- 输入: CSV 行数组, 列名
- 输出: URL 字符串数组
- 过滤: 跳过空值和无效值

### 4. 统计分析层 (Statistics)

#### 4.1 `collectDomainStatistics(urls: string[]): Map<string, DomainEntry>`
**职责**: 统计每个域名的出现次数和对应的 URL 列表
- 输入: URL 数组
- 输出: 域名统计 Map
- 依赖: `extractDomainFromUrl`
- 实现: 使用 Map 和 Set 去重

#### 4.2 `filterByMinimumCount(stats: Map<string, DomainEntry>, minCount: number): DomainStats[]`
**职责**: 过滤出现次数 >= n 的域名
- 输入: 统计 Map, 最小次数
- 输出: 过滤后的域名统计数组
- 转换: Map → Array

#### 4.3 `sortByFrequency(stats: DomainStats[]): DomainStats[]`
**职责**: 按出现次数降序排序
- 输入: 域名统计数组
- 输出: 排序后的数组
- 实现: 纯函数，不修改原数组

### 5. 输出格式化层 (Output Formatting)

#### 5.1 `selectRandomSamples(urls: string[], count: number): string[]`
**职责**: 从 URL 列表中随机选择指定数量的样本
- 输入: URL 数组, 采样数量
- 输出: 随机选择的 URL 数组（去重）
- 算法: Fisher-Yates 洗牌算法

#### 5.2 `formatOutputRows(entries: DomainStats[]): OutputRow[]`
**职责**: 格式化为输出行，填充固定 3 列示例 URL
- 输入: 域名统计数组
- 输出: 输出行数组
- 依赖: `selectRandomSamples`

#### 5.3 `writeCsvOutput(rows: OutputRow[], outputPath: string): Promise<void>`
**职责**: 将数据写入 CSV 文件
- 输入: 输出行数组, 输出路径
- 输出: Promise<void>
- 依赖: `csv-stringify`
- 列名: `域名地址,出现次数,示例url1,示例url2,示例url3`

### 6. 进度跟踪层 (Progress Tracking)

#### 6.1 `class ProgressTracker`
**职责**: 封装进度显示逻辑

```typescript
class ProgressTracker {
  private totalFiles: number = 0;
  private processedFiles: number = 0;
  private totalRows: number = 0;

  startFile(filename: string): void;
  completeFile(rowCount: number): void;
  printProgress(): void;
  getSummary(): ProcessSummary;
}
```

**方法说明**:
- `startFile`: 开始处理文件，打印文件名
- `completeFile`: 完成文件处理，累计行数
- `printProgress`: 打印当前进度
- `getSummary`: 获取总结信息

### 7. 主协调函数 (Main Orchestration)

#### 7.1 `processCsvDomains(dirPath: string, minCount: number): Promise<boolean>`
**职责**: 编排所有功能模块，完成整个流程

**流程**:
```typescript
1. 初始化 ProgressTracker
2. 扫描目录 → getAllCsvFiles()
3. 初始化全局统计 Map
4. 遍历每个文件:
   a. 检测编码 → detectFileEncoding()
   b. 读取解析 → readAndParseCsv()
   c. 提取 URL → extractSourceUrls()
   d. 更新统计 → collectDomainStatistics()
   e. 更新进度 → progressTracker.completeFile()
5. 过滤域名 → filterByMinimumCount()
6. 排序 → sortByFrequency()
7. 格式化输出 → formatOutputRows()
8. 写入文件 → writeCsvOutput()
9. 打印总结 → progressTracker.getSummary()
10. 返回成功/失败
```

**错误处理**:
- 单个文件失败不中断整体流程
- 记录错误日志
- 最终返回处理结果

## 依赖包

### 生产依赖
- `csv-parse` (v5.x): CSV 解析库
- `csv-stringify` (v6.x): CSV 生成库
- `chardet` (v2.x): 文件编码检测
- `iconv-lite` (v0.6.x): 编码转换

### 内置依赖
- `fs/promises`: 异步文件操作
- `path`: 路径处理
- `url`: URL 解析

## 使用示例

```typescript
import { processCsvDomains } from './tools/index.js';

// 处理 CSV 文件
const success = await processCsvDomains(
  '/Users/hadeshe/data/csv-files',  // CSV 文件目录
  2                                   // 最少出现 2 次
);

if (success) {
  console.log('✅ 处理完成！结果已保存到 tools/output.csv');
} else {
  console.error('❌ 处理失败，请查看错误信息');
}
```

## 测试策略

### 单元测试
每个函数都可以独立测试：
- ✅ `extractDomainFromUrl`: 测试各种 URL 格式
- ✅ `selectRandomSamples`: 测试随机采样逻辑
- ✅ `sortByFrequency`: 测试排序正确性

### 集成测试
- 使用小型测试 CSV 文件
- 验证端到端流程
- 检查输出文件格式

## 扩展性

### 易于扩展的功能
1. **新的 URL 提取规则**: 修改 `extractDomainFromUrl`
2. **新的过滤条件**: 添加新的 filter 函数
3. **新的输出格式**: 修改 `formatOutputRows`
4. **新的统计维度**: 扩展 `DomainStats` 接口

### 示例：支持子域名聚合
```typescript
// 只需添加一个新函数，不影响现有代码
function normalizeToMainDomain(domain: string): string {
  // 提取主域名逻辑
}

// 在 collectDomainStatistics 中使用
const normalizedDomain = normalizeToMainDomain(domain);
```

## 性能优化

### 内存优化
- 使用流式处理大文件（如需要）
- Set 去重，避免存储重复 URL
- Map 高效查找

### 并发优化
- 可并行处理多个文件（如需要）
- 使用 `Promise.all` 批量读取

## 错误处理策略

### 文件级错误
- 单个文件失败不影响其他文件
- 记录错误日志，继续处理

### 数据级错误
- 无效 URL 跳过，不中断处理
- 缺失列名时抛出清晰错误

### 致命错误
- 目录不存在
- 无权限访问
- 输出文件写入失败

## 日志输出

### 进度信息
```
[1/10] 处理文件: data1.csv
[2/10] 处理文件: data2.csv (累计 1,234 行)
...
```

### 总结信息
```
==================== 处理完成 ====================
📁 处理文件数: 10
📊 总行数: 12,345
🌐 总域名数: 567
✅ 符合条件的域名数: 123 (出现 >= 2 次)
💾 输出文件: /Users/hadeshe/.../tools/output.csv
==============================================
```

## 实施计划

1. ✅ 创建计划文档
2. ⏳ 安装依赖包
3. ⏳ 实现类型定义
4. ⏳ 实现文件操作层
5. ⏳ 实现数据提取层
6. ⏳ 实现统计分析层
7. ⏳ 实现输出格式化层
8. ⏳ 实现进度跟踪层
9. ⏳ 实现主协调函数
10. ⏳ 添加使用示例和文档

---

**文档版本**: 1.0
**创建日期**: 2025-10-30
**作者**: Claude Code
