import { BRAND_NAME } from "@/constants/config";

export default {
  metadata: {
    title: `{username} - ${BRAND_NAME}`,
    description: `My AI-generated polaroid photos on ${BRAND_NAME}`,
    defaultTitle: `User Profile - ${BRAND_NAME}`,
    defaultDescription: `User profile on ${BRAND_NAME}`,
  },
  generating: 'Generating...',
  generation_failed: 'Generation Failed',
};